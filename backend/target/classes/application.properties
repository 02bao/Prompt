server.port=${PORT:8081}
# Default datasource: use the Render Postgres connection provided by the user.
# You can override by setting the DATABASE_URL, DB_USER or DB_PASS environment variables.
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://dpg-d7katom8bjmc73fdrdcg-a.oregon-postgres.render.com/logindb_st73?sslmode=require}
spring.datasource.username=${DB_USER:postgres_prompt}
spring.datasource.password=${DB_PASS:rRQrsAi3sy5HWxN05HdkHtVWXd4oY3XU}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Important for environments (like Render) that provide a DATABASE_URL in non-JDBC form
# If Render sets DATABASE_URL as postgres://user:pass@host/db you must either:
#  - set an env var named JDBC_DATABASE_URL with the full JDBC URL (jdbc:postgresql://...),
#    or set SPRING_DATASOURCE_URL to the JDBC URL, or edit the DATABASE_URL to start with jdbc:postgresql://
# The application will prefer the JDBC_DATABASE_URL env var if present.
spring.datasource.url=${JDBC_DATABASE_URL:${DATABASE_URL:jdbc:postgresql://dpg-d7katom8bjmc73fdrdcg-a.oregon-postgres.render.com/logindb_st73?sslmode=require}}

# Force the Postgres JDBC driver and Hibernate dialect to avoid auto-detection failures when metadata is unavailable
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
